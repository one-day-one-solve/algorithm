# 3737. Count Subarrays With Majority Element I
Subarray를 전부 탐색해야했던 문제.
이번 문제에서는 배열 nums의 최대 길이가 1000이었으므로 2중 for문을 통한 완전 탐색으로 문제를 해결했다.

# 3699. Number of ZigZag Arrays I
Hard 문제로, dp 유형인 것도 알아채기 쉽지 않았다. AI를 활용하여 문제를 이해했다.
주어진 숫자 범위로 지그재그 array를 만들어야 하는데, 지그재그는 M자 W자 2종류가 존재하지만, 사용하는 숫자 범위가 같으므로 하나의 지그재그를 만드는 경우의 수 * 2로 정답을 낼 수 있다.
주석 [2] 파트에서 for문을 반복할 때마다 기존 dp값을 신규 next_dp에 누적합을 하며 처리가 진행된다. 이 dp와 next_dp의 관계를 이해하는 게 가장 어려웠다.
print를 찍어가면서, 지그재그의 핵심은 이번 자리에 숫자 3을 넣는다고 하면, '숫자 범위 내 3보다 큰 수 혹은 3보다 작은 수가 얼마나 있는가'를 계산하기 위해서 처리하는 과정임을 겨우겨우 이해했다.

# 3020. Find the Maximum Number of Elements in Subset
주어진 숫자들로 특정 subset을 만드는 문제였다.
처음에는 인덱스로 어떻게 탐색해서 풀까 고민하다가, python의 Counter를 떠올린 후
특정 숫자의 제곱값만 탐색하면 되는 문제였기 때문에 로직을 작성하여 풀 수 있었다.